#include <stdio.h>
 int main()
 {
 	float area,raio,pi=3.14;
 	printf("Digite o raio da circunferencia ");
 	scanf ("%f",&raio);
 	area = pi * (raio * raio);
 	printf("O raio da circinferencia e de %f",area);
 	getchar();
 	return 0;
 }
